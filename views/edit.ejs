<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Book</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>Edit "<%= book.title %>"</h1>

    <form action="/edit-book/<%= book.id %>" method="POST" class="book-form">
      <label for="rating">Rating (0–10):</label>
      <input 
        type="number" 
        id="rating" 
        name="rating" 
        min="0" 
        max="10" 
        value="<%= book.rating %>"
      >

      <label for="review">Review:</label>
      <textarea 
        id="review" 
        name="review" 
        rows="4"
      ><%= book.review %></textarea>

      <label for="status">Status:</label>
      <select id="status" name="status">
        <option value="planned" <%= book.status === 'planned' ? 'selected' : '' %>>Want to Read</option>
        <option value="reading" <%= book.status === 'reading' ? 'selected' : '' %>>Currently Reading</option>
        <option value="finished" <%= book.status === 'finished' ? 'selected' : '' %>>Finished</option>
      </select>

      <label for="date_read">Date Finished:</label>
      <input 
        type="date" 
        id="date_read" 
        name="date_read"
        value="<%= book.date_read ? book.date_read.toISOString().slice(0, 10) : '' %>"
      >

      <div style="display: flex; gap: 1rem; margin-top: 1.5rem;">
        <button type="submit" class="edit-button">💾 Save Changes</button>
        <a href="/" class="cancel-button">Cancel</a>
      </div>
    </form>
  </div>
</body>
</html> -->
